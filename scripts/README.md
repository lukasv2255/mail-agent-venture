# scripts/

Pomocné skripty pro vývoj a nastavení. Nespouštějí se automaticky — pouze ručně z terminálu.

| Soubor         | Popis                                                      |
| -------------- | ---------------------------------------------------------- |
| `setup-env.py` | Průvodce pro vytvoření `.env` souboru při prvním nastavení |

## Testovací skripty

Testovací skripty jsou v `tests/<modul>/<projekt>/` — ne zde.
